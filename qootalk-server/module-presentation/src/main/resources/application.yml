server:
  servlet:
    context-path: /api/v1
  port: 8080

spring:
  application:
    name: qootalk
  # 운영환경 빌드 시 프로필 주입
  profiles:
    active: local

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    
